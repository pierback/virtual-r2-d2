{
  "name": "virtual-r2-d2",
  "version": "1.0.0",
  "description": "",
  "main": "app/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":
      "if [$NODE_ENV == 'production']; then ts-node ./app/server.js; else npm run browser && nodemon /app/server.js; fi",
    "dev": "npm run browser && nodemon",
    "browser": "cross-os os",
    "os": {
      "darwin": "open http://localhost:3200/",
      "win32": "start http://localhost:3200/"
    }
  },
  "dependencies": {
    "express": "^4.16.2",
    "ts-node": "^4.1.0",
    "websocket": "^1.0.25",
    "ws": "3.3.3"
  },
  "devDependencies": {
    "cross-os": "^1.2.2",
    "eslint": "^4.11.0",
    "nodemon": "^1.14.7",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2"
  }
}
